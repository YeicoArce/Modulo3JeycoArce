pipeline {
    agent any
    stages {
        stage('Newman test') {
            steps {
                sh '''
                node --version
                cd Modulo3JeycoArce/ci
                newman run ProyectoJeycoArce.postman_collection.json 
                '''
            }
        }
        stage('RestAssured test') {
            steps {
                sh '''
                cd RestAssured
                mvn clean install 
                '''
            }
        }
       
    }
}