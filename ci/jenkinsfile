pipeline {
    agent any
    stages {
        stage('Newman test') {
            steps {
                sh '''
                node --version
                newman run ProyectoJeycoArce.postman_collection.json -e ambiente_examen.postman_environment.json
                '''
            }
        }
       
    }
}